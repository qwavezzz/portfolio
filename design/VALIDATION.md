# Проверка прототипа qwave

**Подготовка GitHub Pages — 14 сентября 2026 года.** Надпись qwave возвращена поверх ASCII-ноутбука автора. Сборка для `/portfolio/` использует общий префикс маршрутов и ассетов; настроен workflow публикации при push в `main`. Перед публикацией прошли 23 проверки корневой версии за 18,7 секунды. Два дополнительных сценария для `/portfolio/` на desktop/mobile прошли за 6,7 секунды: загрузка модели, вход на рабочий стол, папка проектов, SPA-переходы, история и прямое открытие раздела после reload. Они также выявили и подтвердили исправление гонки возврата: позиция восстанавливается по контейнеру рабочего стола, независимо от текущей проекции 3D. Сборка: 0 ошибок/предупреждений/подсказок TypeScript; сохраняется прежнее предупреждение Vite о размере 3D-чанка.

**Последняя проверка — 14 сентября 2026 года, папка «Портфолио».** Первый ярлык рабочего стола открывает папку на той же странице; внутри находится реальный проект «Дары Синергии» со ссылкой `https://dari-sinergii.ru`, открывающейся в новой вкладке. Проверены открытие, закрытие крестиком и Escape с возвратом фокуса, клавиатурный переход из папки, работа без JS через встроенное раскрытие `details` и интеграция с 3D-входом. Финальная сборка: 0 ошибок, предупреждений и подсказок TypeScript; прежнее предупреждение Vite о размере 3D-чанка сохраняется. Полный набор из 23 Playwright-тестов прошёл за 23,3 секунды, включая шесть новых сценариев папки. Адрес внешнего перехода проверен с перехватом новой вкладки; доступность самого внешнего сайта не проверялась.

Finish review `portfolio_folder_finish_review`: **disposition: ship**, `material_fixes: none`, в пределах расширения рабочего стола папкой. Рассмотрены подтверждённые снимки `.impeccable/review/folder-{desktop,tablet,mobile,small}.png` при 1440 × 960, 768 × 900, 390 × 844 и 320 × 640. Детектор `.impeccable/review/folder-detector.json` содержит 77 advisory: 73 по типографической шкале и четыре по цветам; основных замечаний — 0. Эти уведомления не равнозначны 77 визуальным дефектам: CSS использует в том числе прежние служебные размеры и буквальные монохромные оттенки, не полностью представленные в токенах документа.

Документационный проход сопоставил `PRODUCT.md`, контракт `design/SCREENS.md`, `PortfolioFolder.astro`, главную страницу, CSS и снимок desktop с `DESIGN.md` и `.impeccable/design.json`. Папка наследует существующие поверхности, инверсию верхней полосы, прямые углы, шрифты и внешний вид ярлыков; новые системные токены, гарнитуры и растровые ассеты не добавлены. Файлы дизайн-системы сохранены. Отмечено оставшееся расхождение описания с кодом: в `DESIGN.md` ещё сказано, что работы ожидают данных, а `--ease-out` назван неиспользуемым, хотя папка теперь применяет эту существующую переменную. Эти описания и неполная шкала токенов не исправлялись в локальной доработке. Сборка и браузерные тесты в документационном проходе не повторялись.

Далее сохранён журнал проверок от 13 сентября; сведения об отсутствии URL проектов относятся к состоянию до добавления «Даров Синергии».

13 сентября 2026 года. Проверялась собранная версия из `dist/` на `http://127.0.0.1:4322`. Это проверка первого работающего прототипа, не приёмка наполненного публичного портфолио.

Обновление от 13 сентября — ночная комната по референсам 7–8. Финальная сборка: 0 ошибок/предупреждений TypeScript, прежнее предупреждение Vite о размере 3D-чанка. Полный набор из 17 Playwright-тестов прошёл за 28,8 секунды. Новая модель проверена в браузере при 1440 × 960 и 390 × 844; состояния `ready`, ошибок страницы и горизонтального переполнения нет.

Свежий finish review комнаты (`room_finish_review`): **disposition: ship**, `material_fixes: none`; приблизительный предметный состав и мобильное кадрирование признаны допустимыми адаптациями. Документационный проход сопоставил код сцены, её метаданные, CSS и отчёт модели с действующей системой: уточнены границы цветовой палитры, описание комнаты и происхождение вида за окном. Токены и образцы компонентов сохранены; в sidecar синхронизировано только описание согласованной комнаты. Браузерные проверки и сборка в документационном проходе не повторялись.

GLB после weld/dedup/prune/quantize: **2 368 788 байт, 17 mesh-групп, 92 614 треугольников, три текстуры**. Оптимизатор повторно открывает итоговый файл и сравнивает встроенный JPEG с `references/8.jpg` по SHA-256; байты совпадают. Границы основного экрана сохранены с погрешностью менее 0,001 единицы. Результаты записаны в `assets-source/model-report.json`. Внешний декодер не требуется.

Заставки — отдельные Blender-рендеры для landscape и portrait: WebP 90 760 и 68 602 байта соответственно. Проверка происхождения изображений: `embed-prompt --scan public/images` — 3 растра, 0 без происхождения. Геометрия комнаты оригинальная; фотография города предоставлена пользователем.

## Среда

Windows, Node.js 24.18.0, npm 11.16.0, Playwright 1.63.0, Chromium 151.0.7922.34. Браузер запускается headless; для WebGL доступен программный SwiftShader. Ширины функциональных проверок: 360, 390, 768 и 1440 CSS px. Визуальные снимки: 1440 × 960 и 390 × 844, полная высота страниц.

## Сборка и функциональность

`npm.cmd run build` прошла: 0 ошибок, 0 предупреждений TypeScript; собраны `/`, `/about/`, `/contact/`, `404.html`. Vite предупреждает о крупном отдельном чанке 3D (около 946 КБ несжатого JS). Он загружается динамически; это ограничение отмечено, предупреждение не подавлено.

После уточнения сценария первого экрана выполнен полный production-прогон: 17 автоматических сценариев прошли. Добавлены проверки пролёта в монитор на desktop/mobile, обратного движения, resize, SPA-возврата после пролёта и пропуска по кнопке. Для no-JS теста включено reduced-motion, чтобы проверять готовый HTML независимо от плавной прокрутки.

| Сценарий | Результат |
| --- | --- |
| Удержание сцены → приближение → интерактивный HTML, desktop/mobile | Пройден; рабочий стол в конце расположен в начале viewport, ссылки доступны |
| Обратная прокрутка и изменение высоты окна | Пройден; проекция возвращается к прежнему размеру, resize сохраняет рабочий стол |
| Пропуск активного пролёта | Пройден; `/#desktop`, фокус заголовка, без ожидания анимации |
| Ярлык → раздел без перезагрузки документа | Пройден; маркер состояния window сохраняется |
| Возврат, история Назад/Вперёд, восстановление фокуса | Пройден |
| Прямой вход в маршрут и reload | Пройден |
| Клавиатура и фокус заголовка | Пройден |
| Reduced-motion без запроса GLB | Пройден |
| HTML-навигация при отключённом JS | Пройден |
| Ошибка GLB → заставка и рабочие ссылки | Пройден |
| Отключение 3D сохраняется после reload | Пройден |
| Ошибка загрузки внутреннего раздела → сообщение и повтор | Пройден |
| Неизвестный адрес → 404 и возврат | Пройден |
| Отсутствие горизонтального переполнения на 360/390/768/1440 | Четыре проверки пройдены для всех трёх маршрутов |
| Пустые состояния, отсутствие фиктивных ссылок, noindex | Пройден |

В таблице некоторые автоматические сценарии объединены по смыслу. Видимые ссылки в проверенных раскладках имеют высоту не меньше 44px.

## Визуальная проверка

Для новой механики входа просмотрены промежуточные состояния и полноэкранный HTML на 1440 × 960 и 390 × 844. Снимки: `.impeccable/review/flight-desktop-{0,0.45,0.9,1}.png` и `flight-mobile-{0,0.45,0.9,1}.png`. HTML совмещается с экраном, затем занимает весь viewport. При проекции в широкий физический монитор мобильная раскладка временно меняет пропорции; конечная раскладка использует обычные размеры CSS. Исправлены горизонтальное переполнение проекции и возврат в пролёт при resize. Необработанных ошибок страницы в новых сценариях нет.

Снимки первого viewport обновлены для новой комнаты: `.impeccable/review/desktop-hero.png` и `mobile-hero.png`. Снимки стадий пролёта также пересняты после изменения модели. Внутренние разделы в этой доработке не менялись.

Просмотрены шесть полных снимков:

- [Главная desktop](../.impeccable/review/desktop.png) и [mobile](../.impeccable/review/mobile.png).
- [Обо мне desktop](../.impeccable/review/desktop-about.png) и [mobile](../.impeccable/review/mobile-about.png).
- [Контакты desktop](../.impeccable/review/desktop-contact.png) и [mobile](../.impeccable/review/mobile-contact.png).

На главной в обоих размерах подтверждено `data-scene-state="ready"`; необработанных ошибок страницы не зарегистрировано. В финальных снимках нет горизонтального переполнения. Исправлены переполнение сцены и наложение непрозрачной заставки на WebGL; мобильные ярлыки и графика размещены в потоке.

В предыдущей итерации механический проход Impeccable 4.3.1 `detect --json src` выполнен один раз: `[]`. Тогда независимый finish reviewer просмотрел все шесть снимков, контракт и исходники: **disposition: ship** для разрешённого первого прототипа, `material_fixes: none`. Это заключение предшествует изменению пролёта. Новый переход проверен по animate/polish на снимках и браузерными тестами. Направление задано шестью пользовательскими референсами.

## Ассеты и ограничения

Актуальные размеры модели и заставок указаны в начале документа. Изображения созданы из Blender-рендера, включающего предоставленную фотографию города; происхождение записано в метаданных/sidecar. 3D и основной интерфейс используют локальные ресурсы.

Снимки mobile — эмуляция ширины, не проверка на физическом телефоне. Safari, Firefox, iPhone, Android, масштабирование текста до 200%, измерения LCP/CLS по профилю ТЗ и реальный FPS пока не проверены. Работа с предоставленными внешними проектами и контактами требует повторной проверки после их добавления. Сейчас соответствующих URL нет по указанию автора.

Имя qwave подтверждено; остальное наполнение ещё готовится. Сайт не публиковался. Домен, canonical, Open Graph изображение, robots.txt и sitemap следует завершить после наполнения и выбора хостинга.
